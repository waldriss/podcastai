import { QuoteDetailPlayerProps } from '@/lib/types/quote'
import React from 'react'

const QuoteDetailPlayer = ({}:QuoteDetailPlayerProps) => {
  return (
    <div>QuoteDetailPlayer</div>
  )
}

export default QuoteDetailPlayer